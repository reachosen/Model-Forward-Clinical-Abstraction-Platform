# ============================================================================
# DEV Environment Configuration
# ============================================================================
# Usage: Source this file before running deployment scripts
# Example: source environments/dev.config && snowsql -f snowflake_factory.sql
# ============================================================================

# Environment
export SNOWFLAKE_ENV=DEV

# Database Configuration
export SNOWFLAKE_DATABASE=CLINICAL_ABSTRACTION_DEV
export SNOWFLAKE_WAREHOUSE=CLINICAL_WH_DEV
export SNOWFLAKE_WAREHOUSE_SIZE=SMALL

# Connection (set these in your environment or CI/CD)
# export SNOWFLAKE_ACCOUNT=your_account
# export SNOWFLAKE_USER=your_user
# export SNOWFLAKE_PASSWORD=your_password
# export SNOWFLAKE_ROLE=SYSADMIN

# Features
export LOAD_SEED_DATA=true
export ENABLE_VALIDATION=true

# Application Configuration (for reference)
export API_BASE_URL=http://localhost:5000
export UI_BASE_URL=http://localhost:3000
export MODE=TEST
